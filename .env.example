# ===========================================
# ETH Trading Bot - Environment Configuration
# ===========================================

# ============================================
# SLACK CONFIGURATION
# ============================================

# Bot User OAuth Token (starts with xoxb-)
# Get from: https://api.slack.com/apps → Your App → OAuth & Permissions
SLACK_BOT_TOKEN=xoxb-your-bot-token-here

# App-Level Token (starts with xapp-) - ONLY for Socket Mode
# Get from: https://api.slack.com/apps → Your App → Socket Mode → Generate Token
SLACK_APP_TOKEN=xapp-1-your-app-token-here

# Socket Mode (true = local dev, false = Heroku production)
# Local dev: true (no webhooks needed!)
# Heroku: false (uses HTTP webhooks)
SLACK_SOCKET_MODE=true

# Enable/disable Slack integration
SLACK_ENABLED=true

# Optional: Default channel
SLACK_CHANNEL_ID=
SLACK_CHANNEL_NAME=

# ============================================
# REDIS
# ============================================
REDIS_URL=redis://localhost:6379

# ============================================
# OPENAI
# ============================================
OPENAI_API_KEY=sk-your-openai-api-key

# OpenAI Model Selection
# Options: gpt-4o, gpt-4o-mini, gpt-4-turbo, gpt-3.5-turbo
# Default: gpt-4o-mini (fast, cheap, good for trading)
# For best analysis: gpt-4o or gpt-4-turbo (slower, more expensive)
OPENAI_MODEL=gpt-4o-mini

# ============================================
# BINANCE (optional - uses public API if empty)
# ============================================
BINANCE_API_KEY=
BINANCE_API_SECRET=
BINANCE_TESTNET=true

# ============================================
# JWT & SECURITY
# ============================================
JWT_SECRET=your-random-jwt-secret-here-change-this
REDIRECT_URI=http://localhost:8080

# ============================================
# DATABASE
# ============================================
# Default: H2 in-memory (no persistence)
# For persistence: Use PostgreSQL profile

# PostgreSQL Configuration (when using -Dspring-boot.run.profiles=postgres)
# Default values match docker-compose.yml
DATABASE_URL=jdbc:postgresql://localhost:5432/demospring
DB_USERNAME=demospring
DB_PASSWORD=demospring123

# Or leave empty to use H2 in-memory (data lost on restart)
# DATABASE_URL=
# DB_USERNAME=
# DB_PASSWORD=

# ============================================
# SALESFORCE (if needed)
# ============================================
SF_LOGIN_URL=
SF_CLIENT_ID=
SF_USERNAME=
SF_SESSION_TIMEOUT=
SF_CONNECTOR_NAME=

# ============================================
# NETATMO (if needed)
# ============================================
NETATMO_CLIENT_ID=
NETATMO_CLIENT_SECRET=
